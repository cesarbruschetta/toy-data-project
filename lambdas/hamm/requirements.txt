# Produção — boto3 já está disponível no runtime Python da AWS Lambda
boto3==1.34.69

# PyIceberg para escrita em tabelas Iceberg (S3 Tables)
pyiceberg[s3tables]==0.9.0
pyarrow==18.1.0

# Testes locais
pytest==8.3.5
pytest-mock==3.14.0
moto[s3,sqs]==5.0.7
